### Data Portability API Mocker

To run the Data Portaility API mocker:

- run: `docker compose up -d httpmock`to start the mocker
- run: `cargo run`
- the mocker is now running with the mocked paths specified in: `src/main.rs`
